% 设置飞行器的位置和姿态
position = [0, 10, 10];    % 飞机位置
attitude = deg2rad([0, 0, 0]);    % 飞机姿态：roll pitch yaw, 弧度rad;rad = deg * pi / 180
tilt_angle = [90, 45];     % 电机倾转角 顺序为右，左，向倾转为正；尾部固定。为c；
 
% % 调用函数进行可视化
% WingPlot(position, attitude, wingspan, length, tail_span, engine_pos);
% planeplot_ttr(position, attitude,tilt_angle); % 完成倾转---
planeplot_ttr_test(position, attitude,tilt_angle);